[package]
name = "santui-core"
version = "0.2.5"
edition = "2021"

[dependencies]
ratatui = "0.30"
crossterm = "0.29"
santui-registry = { path = "../registry", version = "0.2.5" }
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[dev-dependencies]
proptest = "1"
