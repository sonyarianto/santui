[package]
name = "santui-auth"
version = "0.2.39"
edition = "2024"
rust-version = "1.85"
description = "Santui authentication — GitHub OAuth client with device flow support"
license = "AGPL-3.0-only"
publish = false
repository = "https://github.com/sonyarianto/santui"

[dependencies]
santui-core = { path = "../core", version = "0.2.39" }
ureq = "3"
url = "2"
serde = { workspace = true }
serde_json = { workspace = true }
dirs = "6"
