# Per-namespace copy of the cluster's http->https redirect (same as apps/db).
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: redirect-to-https
  namespace: santui
spec:
  redirectRegex:
    regex: "^http://(.*)"
    replacement: "https://${1}"
    permanent: true
